<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dacasa.nyimbozakristo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sda_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/sda_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.NyimboZaKristo">
        <activity android:name=".MainActivity2"></activity>
        <activity android:name=".NyimboSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".NyimboFragment"></activity>
    </application>

</manifest>